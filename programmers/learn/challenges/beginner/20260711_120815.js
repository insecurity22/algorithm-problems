function solution(n) {
    let pizza = 1;
    
    while((pizza * 6) % n !== 0) {
        pizza++;
    }
    
    return pizza;
}

// 1. 최소공배수를 이용한 풀이
// - 피자는 한 판에 6조각이므로, 필요한 전체 조각 수는 6과 n의 최소공배수이다.
// : 최소공배수(LCM) 공식 = (a * b) / GCD(a, b)
// : 최대공약수(GCD)는 유클리드 호제법으로 구한다. = GCD(a, b) = GCD(b, a % b)

// 피자는 6조각 단위로 늘어나고, n명이 같은 개수의 조각을 먹어야 한다.
// 따라서 6의 배수이면서 n의 배수인 가장 작은 수(최소공배수)를 찾으면 된다.

// LCM 예) n = 4명
// 1판 → 6조각 (6 % 4 = 2) ❌
// 2판 → 12조각 (12 % 4 = 0) ✅
// 최소 12조각이 필요하고, 피자는 한 판에 6조각이므로 12 / 6 = 2판이 필요하다.

// GCD 예) GCD(24, 18)
// 24 % 18 = 6
// GCD(24, 18) = GCD(18, 6) = 6

// function solution(n) {
//     // 최대공약수(GCD) - 유클리드 호제법
//     const gcd = (a, b) => b ? gcd(b, a % b) : a;

//     // 최소공배수(LCM)
//     const lcm = (6 * n) / gcd(6, n);

//     // 필요한 전체 조각 수를 피자 한 판(6조각)으로 나눈다.
//     return lcm / 6;
// }