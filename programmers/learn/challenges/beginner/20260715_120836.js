function solution(n) {
    let count = 0;
    for(let i = 1; i <= n; i++) {
        if(n % i === 0) count++;
    }
    return count;
}

// 1. 더 효율적인 풀이
// - 약수는 항상 (작은 약수, 큰 약수) 형태의 쌍으로 존재하므로, 작은 약수를 찾으면 큰 약수도 함께 찾은 것이 되어 +2를 한다. 
//   따라서 √n까지만 확인해도 모든 약수를 구할 수 있다.
//   예) 24의 약수: (1, 24), (2, 12), (3, 8), (4, 6)

// - 단, 16처럼 (4, 4)와 같이 같은 수가 짝이 되는 경우에는 약수를 한 번만 세므로 +1만 한다.
// - 시간 복잡도는 기존 풀이가 O(n), 현재 풀이가 O(√n)으로 더 효율적이다.

/*
    for (let i = 1; i * i <= n; i++) {
        if (n % i === 0) {
            count += (i * i === n) ? 1 : 2;
        }
    }
*/